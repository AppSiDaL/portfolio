export interface exp {
  logo: string;
  name: string;
  enterprise: string;
  journey: string;
  startDate: string;
  endDate: string;
  dif?: string;
  location: string;
  modality: string;
  description?: string;
  aptitudes: string[];
}

export interface project {
  logo: string;
  name: string;
  enterprise: string;
  journey: string;
  startDate: string;
  endDate: string;
  dif?: string;
  location: string;
  modality: string;
  description?: string;
  aptitudes: string[];
}
