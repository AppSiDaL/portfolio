export interface exp {
  logo: string;
  name: string;
  enterprise: string;
  journey: string;
  startDate: string;
  endDate: string;
  dif?: string;
  location: string;
  modality: string;
  aptitudes: string[];
}
